# МСП: инструкции и дока агенту

Черновик на обсуждение. Вопросы в конце адресованы тому, кто на MagicPro много
верстал: часть решений — это опыт использования, а не разработки.

## Состояние

Устарело. Вопросы, которые тут стояли, закрыты файлами
`docs/ru/agentsInstruction/` — `mcpInstructions.md`, `mcpDocs.md`, `blade.md`:
там и текст инструкций, и устройство оглавления, и приёмы вёрстки.

Сделанное по ним лежит в `docs/toDo/mcpAgentDocs/`, готовое, но не подключённое —
см. `README.md` там же.

Осталось решить одно: ресурсы (`magicpro://docs/{path}`) делаем или убираем
`resources` из объявленных возможностей сервера. Сейчас он их обещает, а их ноль.
