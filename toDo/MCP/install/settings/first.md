1. Спроси у пользователя URL MCP-сервера.
2. Добавь MCP-сервер **только в локальный конфигурационный файл текущего проекта**.
3. Не изменяй глобальные конфигурационные файлы пользователя.
4. После изменения конфига попроси пользователя перезапустить агента / текущую сессию, чтобы MCP подключился.

Транспорт Streamable HTTP, авторизация `Authorization: Bearer <токен>`. Токен приходит **только** из переменной окружения `SOLNUH_TOKEN`.
