Что поправить под нас:

1. Адрес вписывать при сборке архива, а не через first.md. Архив собирает сайт на странице /a_dmin/mcp, и свой адрес
   (https://сайт/mcp/magicpro) он знает. Сразу впишет его в .mcp.json и .codex/config.toml. Тогда пропадает весь танец
   «введи любое число → агент ругается → выполни first.md → перезапусти»: распаковал, запустил, ввёл токен. first.md
   не нужен.

   > вещь правильная, но тогда архив будет выглядеть совершенно по другому. Архив для виндов архив для линукса. Предлагаю архив для линукса пока не делать, занести его в риски, его тестировать некогда. Резюме. Оставляем архив для виндов, делаем адрес уже готовый.

2. Имена. SOLNUH_TOKEN → MAGICPRO_TOKEN, сервер solnuh → magicpro.
   > Да
3. AGENTS.md переписать под наш МСП: - наши инструменты: list-docs/get-doc с about, get-article, save-article, move-article, двухшаговый delete-article,
   ленты; - из их правил оставить общие: «вопрос — не задача», не удалять молча, не переименовывать; - их специфику выкинуть: template, get-template-manual, removedInlinePhp — у нас такого нет; - таблицу ошибок 401 заполнить нашими текстами из Token.php.
   > Да
4. Где лежит. Если положить в docs/ru/mcp/install/, раздел «Документация» покажет AGENTS.md, CLAUDE.md и readme.md в
   списке «Вне дерева». Либо научить обход пропускать install/, либо положить рядом с доками, но не в docs/ru —
   например, в docs/mcp-install/. Мне больше нравится второе: это не страница доки, а заготовка.
   > Да docs/mcp-install/
5. Linux. В zip у .sh может потеряться флаг исполнения. В readme.md писать запуск через bash runClaude.sh, либо
   ставить флаг при сборке архива.

   > Нужно отдельную инструкцию для линукса

6. Для архива нужно PHP-расширение zip (ZipArchive). Надо проверить, что оно есть на боевом.
   > да надо проверить, если нет вместо архива выводить сообщение ПХП... такое то расширение не установлено

Когда это заработает, можно убрать старое:

- поиск mcpInstructions в MagicProServer;
- docsTmp/agentSetup/ с bat-файлами;
- разделы про Codex, mpro-codex() и bat в mcp/use.md.

Делать по этой схеме?
