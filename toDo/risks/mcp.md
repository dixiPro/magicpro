# МСП: незакрытое

Инструменты агента. Подробный разбор — `toDo/Archive/mcp/risks-full.md`, номер в
скобках — оттуда.

Открыто: 1. Список всех подсистем — `toDo/risks.md`.

---

1. **Архива агента для Linux нет.** `install.zip` со страницы MCP — только
   Windows (`.bat`). Для Linux нужны свои лаунчеры `.sh`, флаг исполнения
   (в zip он теряется, либо запуск через `bash runClaude.sh`) и отдельная
   инструкция. Не сделано: тестировать некогда, лучше одно хорошо.

---

Сознательно не делаем: журнал результатов вызовов (5) — лог превратится в
свалку; выпуск токена (4) — работает; подтверждение удаления против
параллельных правок (3) — система не многопользовательская.
