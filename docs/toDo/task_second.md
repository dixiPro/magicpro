## Почтовая система

- Вебхук Амазона (`/awsHook`) настроен на временный адрес.
- В диагностике видно, что планировщик тикает. Что письма реально уходят из
  очереди — отдельная строка, её нет.

Сделано 2026-08-11: `sendQueue` добавлен в расписание, отладочный `dump()` из
`sendQueueLocked()` убран, на `/a_dmin` появился статус планировщика.

## МСП для лент

Инструмент `src/Mcp/Tools/FeedApiTool.php` не написан, ТЗ лежит в
`docs/TZ_lenta/TZ_mcp.md` с пометкой «План. Кода пока нет». Агент ходит только к
статьям, к лентам — нет.

## Ленты

- Экспорт и импорт лент. В ТЗ отдельный этап, команд в `$map` нет. Пока их нет,
  удаление ленты необратимо.
- Поля-файлы: сделаны только изображения. Документы и видео — отдельным типом
  поля, позже.

## Установка

Давно не проверялась.

## Документация

- `docs/ru/inside.md`, таблица настроек: у группы `RESIZE` написано «пока нигде,
  код появится позже». Ресайзер сделан, строку править.
- По почте, статике и МСП отдельных документов в `docs/ru/` нет.

## Мелочи

- `templateName` в articles — поле есть, не используется.
- ClassBased-компонент и Livewire-контроллеры помечены в `docs/ru/common.md` как
  опытная эксплуатация, в проектах не применяются, формат может измениться.
- TODO в `src/Config/MagicGlobals.php:32` — перевести `MAGIC_*` и прочие
  константы на `config('magicpro.*')`.
- Перенести умолчания качества по форматам из `ImageJob` в `ImageEncoder`.

## Сознательно не делаем

Из разделов «Не реализовывать» в ТЗ лент: фильтрация по содержимому `json`,
динамическое создание SQL-таблиц, пользовательские типы полей, миграции
структуры, конструктор схем, вычисляемые поля, сложные валидаторы,
мультиязычность данных (интерфейс — да, данные — нет).
