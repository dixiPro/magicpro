# ТЗ v2. Модуль «Ленты» (Feeds). Две таблицы

Версия 2 покрывает хранение и CRUD. Связи между лентами вынесены в отдельное ТЗ —
`task18_v2a.md`. Исходная версия и ответы на вопросы — `task18.md`.

## Цель

Универсальный модуль хранения данных: произвольные сущности (блог, новости, FAQ, отзывы,
сотрудники, вакансии, платежи) без написания нового CRUD.

Главная цель — максимально простой движок, покрывающий 90–95% задач небольших сайтов.

MagicPro не является конструктором баз данных. Это механизм хранения с фиксированной
структурой таблиц и API, внешне похожим на Laravel Query Builder. Разработчик работает с
логическими именами полей, физические колонки скрыты.

Везде, где можно опереться на Laravel, опираемся на Laravel: Eloquent, пагинатор, касты,
Spatie Media Library.

## Структура БД

### Таблица `feeds`

Описание ленты. Метаданные, а не данные — по ней не ищут, поэтому описание полей лежит
одним JSON-столбцом.

| Колонка      | Тип    | Назначение                                |
| ------------ | ------ | ----------------------------------------- |
| `id`         | bigint | Первичный ключ                            |
| `uid`        | string | Код ленты, по нему идёт обращение из кода |
| `title`      | string | Человекочитаемое название                 |
| `schema`     | json   | Описание полей                            |
| `lock`       | bool   | Схема закрыта от правки в админке         |
| `created_at` |        |                                           |
| `updated_at` |        |                                           |

#### Формат `schema`

Массив, порядок элементов = порядок полей в форме.

```json
{
  "fields": [
    { "code": "title", "column": "string_1", "label": "Заголовок", "widget": "input" },
    { "code": "slug", "column": "string_2", "label": "URL", "widget": "input", "unique": true },
    { "code": "content", "column": "text_1", "label": "Текст", "widget": "textarea" },
    { "code": "published_at", "column": "bigint_2", "label": "Дата", "widget": "date", "cast": "datetime" },
    { "code": "price", "column": "bigint_3", "label": "Цена", "widget": "input", "cast": "money" },
    { "code": "user_id", "column": "bigint_1", "label": "Автор" },
    { "code": "published", "column": "bool_1", "label": "Опубликовано", "widget": "checkbox" }
  ]
}
```

| Ключ     | Обязателен | Назначение                                               |
| -------- | ---------- | -------------------------------------------------------- |
| `code`   | да         | Логическое имя, им пользуется разработчик                |
| `column` | да         | Физическая колонка в `feed_items`                        |
| `label`  | нет        | Подпись в админке, по умолчанию = `code`                 |
| `widget` | нет        | Тип поля в форме, по умолчанию выводится из типа колонки |
| `cast`   | нет        | Преобразование значения при чтении и записи              |
| `unique` | нет        | Значение уникально внутри ленты                          |

#### `cast`

Новых физических типов не вводим. Дата и деньги живут в `bigint`, а Eloquent приводит их
на лету:

| `cast`     | Хранится                               | Отдаётся в PHP                        |
| ---------- | -------------------------------------- | ------------------------------------- |
| `datetime` | unix timestamp в `bigint`              | `Carbon`                              |
| `money`    | целое в минимальных единицах (копейки) | int, форматирование на стороне вывода |
| нет        | как есть                               | как есть                              |

Такое хранение сортируется и индексируется наравне с обычными числами.

### Таблица `feed_items`

Хранит данные. Структура фиксированная.

| Колонка                  | Назначение                     |
| ------------------------ | ------------------------------ |
| `id`                     | Первичный ключ                 |
| `feed_uid`               | К какой ленте относится запись |
| `string_1` … `string_10` | Строковые поля                 |
| `text_1` … `text_5`      | Текстовые поля                 |
| `bigint_1` … `bigint_5`  | Числовые поля                  |
| `bool_1` … `bool_5`      | Булевы поля                    |
| `json`                   | Дополнительное поле            |
| `position`               | Позиция внутри `feed_uid`      |
| `created_at`             |                                |
| `updated_at`             |                                |

#### Правила

- **`bool_*` никогда не `NULL`.** При создании записи все булевы колонки получают `0`.
  Иначе записи, созданные до появления поля, не попадут ни в выборку по `true`, ни по
  `false`.
- **`bool_1` — всегда флаг видимости** (`published`, `active`, `visible`). Соглашение
  нужно, чтобы одним составным индексом покрыть главный запрос сайта, а не заводить по
  индексу на каждый из пяти булевых слотов.
- **`position` при создании = `max(position) + 1`** внутри своей ленты. Не ноль — иначе у
  всех новых записей одинаковая позиция.
- **`json`** — законное место для полей, которые не нужны в поиске. Колонки нужны для
  фильтрации и сортировки; всё, что просто хранится и выводится, может лежать в `json`.

### Медиа

Spatie Media Library. Количество изображений не ограничено. Требует, чтобы запись была
Eloquent-моделью — это и заложено в архитектуру.

### Индексы

Составные, порядок колонок: сначала равенство, потом сортировка, в конце `id`.

| Индекс                                          | Для чего                                      |
| ----------------------------------------------- | --------------------------------------------- |
| `(feed_uid, position, id)`                      | Список ленты по порядку                       |
| `(feed_uid, bool_1, position, id)`              | Список опубликованного — главный запрос сайта |
| `(feed_uid, created_at, id)`                    | Список по дате                                |
| `(feed_uid, string_1)` … `(feed_uid, string_3)` | Поиск по строковому полю, например `slug`     |
| `(feed_uid, bigint_1)` … `(feed_uid, bigint_3)` | Поиск по числовому полю                       |

Текстовые поля не индексируются.

`id` в конце сортировочных индексов — не про скорость, а про корректность: см. раздел
«Пагинация».

## Админка

Раздел **Ленты**.

- список лент
- создание ленты
- удаление ленты
- изменение названия
- настройка полей: логическое имя, подпись, виджет
- переключатель `lock`

При `lock = true` правка схемы в админке недоступна. При `lock = false` оператор
распоряжается схемой свободно: переименовал поле — поедет его собственный CRUD, это его
дело. **Данные при правке схемы не трогаются никогда**: ни очистки колонок, ни переноса
значений.

### CRUD записей

Форма строится по `schema`. `widget` берётся из описания поля, если не задан — выводится
из типа колонки: `string` → `input`, `text` → `textarea`, `bool` → `checkbox`,
`bigint` → `input`.

## PHP API

### Чтение

```php
MproHelper::feed('blog')
```

Возвращает `FeedQuery`. Поддержать: `where()`, `orWhere()`, `whereIn()`, `orderBy()`,
`limit()`, `offset()`, `first()`, `find()`, `get()`, `count()`, `exists()`, `paginate()`.

```php
$post = MproHelper::feed('blog')
    ->where('slug', $slug)
    ->where('published', true)
    ->first();
```

Разработчик никогда не работает с `string_1`, `text_3`, `bigint_2` — только с логическими
именами. Преобразование `slug` → `string_2`, `published` → `bool_1` происходит внутри
`FeedQuery` по `schema` ленты.

### Запись

В первой версии ТЗ этот раздел был пропущен.

```php
// создание
$id = MproHelper::feed('blog')->create([
    'title'     => 'Привет',
    'slug'      => 'privet',
    'published' => true,
]);

// изменение по условию
MproHelper::feed('blog')
    ->where('slug', 'privet')
    ->update(['published' => false]);

// изменение записи
$post->title = 'Другой заголовок';
$post->save();

// удаление
MproHelper::feed('blog')->where('slug', 'privet')->delete();
$post->delete();
```

Правила записи:

- неуказанные поля сохраняют текущее значение
- поле, которого нет в `schema` ленты — ошибка, а не молчаливое игнорирование
- `position` при создании выставляется автоматически
- `unique` проверяется кодом перед записью

### Получение данных

Запись отдаёт свойства по логическим именам:

```php
$post->title;
$post->slug;
$post->content;
$post->user_id;
```

а не `$post->string_1`.

Служебные атрибуты названы так, чтобы не отбирать у оператора удобные имена — логические
поля `id`, `position`, `json` остаются свободными:

| Служебный атрибут      | Физическая колонка |
| ---------------------- | ------------------ |
| `$post->mproId`        | `id`               |
| `$post->mproPosition`  | `position`         |
| `$post->mproJson`      | `json`             |
| `$post->mproCreatedAt` | `created_at`       |
| `$post->mproUpdatedAt` | `updated_at`       |

При обращении сначала ищется поле в `schema`, потом служебный атрибут.

### Пагинация

Используется штатный `LengthAwarePaginator`, никаких своих велосипедов.

```php
$posts = MproHelper::feed('blog')
    ->where('published', true)
    ->orderBy('position')
    ->paginate(10);
```

В блейде работает `{{ $posts->links() }}`.

Два требования, без которых пагинация тихо ломается:

1. **Сортировка всегда однозначна.** К любому `orderBy` автоматически добавляется `id`
   вторым ключом. Иначе при одинаковых `position` одна запись покажется на двух страницах,
   а другая не покажется нигде.
2. **Номер страницы приходит строкой из URL** — приводится к int и зажимается в
   `[1, lastPage]`. Значения `page=abc`, `page=-5`, `page=0` не должны ломать выборку.

Для длинных лент предусмотреть `simplePaginate()` — он не делает второй запрос `COUNT(*)`.

Статья, выводящая ленту, должна иметь `routeParams.getEnable = true`, иначе параметр `page`
до неё не доедет.

## Архитектура

Отдельный неймспейс, а не свалка в `MproHelper`:

| Класс       | Ответственность                                                                                      |
| ----------- | ---------------------------------------------------------------------------------------------------- |
| `Feed`      | Eloquent-модель ленты, разбор `schema`                                                               |
| `FeedItem`  | Eloquent-модель записи, подмена имён атрибутов, Spatie Media                                         |
| `FeedQuery` | Построитель запросов, **единственное место**, где логические имена превращаются в физические колонки |
| `API_Feeds` | Командный API для админки, в стиле `AbstractApi`                                                     |

`MproHelper::feed($uid)` — строчка-обёртка над `FeedQuery`, чтобы блейды звали без `use`.

Остальной код о физических колонках не знает.

### Целостность

Внешних ключей не заводим. Чистка при удалении ленты делается кодом. Операция редкая,
скорость значения не имеет.

## Не реализовывать в этой версии

- связи между лентами — отдельное ТЗ `task18_v2a.md`
- динамическое создание SQL-таблиц
- пользовательские типы полей
- миграции структуры
- конструктор схем
- вычисляемые поля
- сложные валидаторы
- мультиязычность данных (интерфейс — да, данные — нет)

---

## Вопросы по этому ТЗ

**1. Можно ли менять `uid` после создания?** Вопрос остался без ответа. Если менять
разрешено, все `feed_items.feed_uid` осиротеют. Предлагаю: `uid` задаётся один раз и
дальше не редактируется, `title` меняется свободно. Набор символов — `^[a-z0-9_-]+$`.

> нет это поле менять нельзя

**2. Уникальность значений — как проверяем?** Флаг `unique` в схеме я заложил, но
уникальным индексом это не сделать: индекс `(feed_uid, string_2)` заставит быть уникальным
`string_2` у всех лент сразу, а другая лента может держать там неуникальные данные.
Значит проверка кодом перед записью. Согласны, что этого достаточно, или нужен уникальный
индекс с оговоркой «слот занят под уникальное поле во всех лентах»?

**3. Сколько индексов заводить.** Проиндексировать все 10 строковых и 5 числовых слотов —
это 15 вторичных индексов на таблицу, они замедляют запись и раздувают базу. Я заложил
первые три слота каждого типа. Альтернатива — флаг `index` в описании поля и создание
индексов по факту. Второе гибче, но требует ALTER TABLE при правке схемы, а мы от этого
уходим. Оставляем первые три?

**4. Что делает `lock`?** Закрывает только правку схемы, или ещё и удаление ленты? Я
заложил первое.

> и правку и удаление ленты

**5. Удаление непустой ленты** — разрешить с удалением всех записей, или запретить, пока
записи есть? Второе безопаснее, но требует ручной чистки.

> разрешить

**6. Нужен ли `soft delete` для записей?** Для отзывов и комментариев обычно нужен. Это
одна колонка `deleted_at` и трейт Laravel, но добавлять её потом больнее, чем сразу.

> да мысль хорошая, хранить все, заводим поле

**7. Служебные имена `mproId`, `mproPosition`.** Это мой вариант «заковыристых названий».
Устраивает, или предпочитаете другой префикс?

> да можно еще заковыристее

**8. Ещё типы.** Вы спрашивали, какие добавить. Мой ответ: **физически — ни одного**.
Дата и деньги ложатся в `bigint` и покрываются кастами, а любой новый физический тип это
навсегда: он попадает в таблицу, в индексы и в миграции. Единственный кандидат, о котором
стоит подумать, — `decimal`, если появятся веса, проценты или координаты, где копейки
неудобны. Но и это решается хранением целого с фиксированным множителем.

**9. `json` в запросах.** Доступен как `$post->mproJson`. Фильтровать по нему в этой
версии не даём — согласны? Laravel умеет `where('json->key', ...)`, но в SQLite это
работает иначе, чем в MySQL, и переносимость пострадает.

> json как резервное хранилище. Нужно что то хранить, что не лезет, вот иди в джсон. Делай отдельную админку, если надо.
