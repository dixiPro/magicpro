# ТЗ. Модуль «Ленты» (Feeds). Редактирование ленты

code название (мозжно редактировать)

## сейчас сделай только отображение и отображение строковых параметров. Все остальные не делай.

Сейчас на экране работает только группа String: её поля показываем и редактируем. Остальные
группы не делаем вовсе.

## Как отображаеть

Выводим по группам.

### Порядок полей

Порядок полей пока фиксирован: сначала группа Дата, потом Линк, потом Бул, потом String,
потом Bigint, потом ДЖСОН.

### Внутри группы

Добавить можно в первый свободный.
Т.е. если в группе link полей нет, добавится в первое поле.

## Пример

| Code | Label    | default | Unic | validation                            |
| ---- | -------- | ------- | ---- | ------------------------------------- |
| Name | Название | NULL    | yes  | required integer min:100 max:1000 (?) |

<добавить>

смотри я специально в текстовое поле добавить required integer, плевать, хочет админ такое, получите. Мы не учебный центр. А вот дать хелп и типовые схемы это будет правильно

### Примеры validation

Поле `validation` — свободный текст, строка правил Laravel. Что оператор написал, то и
уедет в схему; разбирать её будет штатный валидатор при сохранении записи.

| Что нужно                           | Строка                                 |
| ----------------------------------- | -------------------------------------- |
| Обязательная строка, 3–255 символов | `required\|string\|min:3\|max:255`     |
| Необязательный текст                | `nullable\|string\|max:5000`           |
| Обязательное целое в диапазоне      | `required\|integer\|min:100\|max:1000` |
| Почта                               | `required\|email`                      |
| Дата или дата-время                 | `required\|date`                       |
| Флаг                                | `required\|boolean`                    |
| Одно из значений                    | `required\|in:new,used,broken`         |
| По маске                            | `required\|regex:/^[A-Z]{2}-\\d{4}$/`  |

Правила пишутся через `|`, порядок значения не имеет. `required` — часть той же строки,
отдельного ключа под него нет. `nullable` нужен, чтобы пустое значение не считалось
ошибкой.

Уникальность правилом Laravel не задаётся — для неё есть отдельная колонка `Unic`: слот
общий для всех лент, и `unique:magicPro_feed_items,__string_2` проверял бы всю таблицу.

Полный список правил — в документации Laravel:
<https://laravel.com/docs/validation#available-validation-rules>

Эти же примеры показывает подсказка «?» рядом с заголовком группы.

### ДЖСОН

будет отдельное тз, пока выводим текстАреа

### Когда сохраняем

Схема по ТЗ сохраняется целиком, одной командой со всеми проверками.

### Лента с записями.

Переименовывать `code` уже нельзя — поле блокируется, и рядом нужна подсказка почему. Вывести ? и туда текст
** Сделать общий компонент help ** положить его в packages\dixipro\magicpro\admin\js\app\CommonCom

Компонент простой: иконка вшита в него, берём бутстраповский вопрос. По клику открывается
модальное окно, из которого убрано всё лишнее. Текст прилетает пропсом.

## связь

Установка связи в модальном окне.
Да опперато выбирает целевую ленту, а потом поле code, но в данные пишется **ФИЗИЧЕСКОЕ поле.**

## Не решенные и подвешенные вопросы

Хелп
Правила валидации

Делаем редактирование то, что описано в data (JSON)

Для JSON целое, строку, булеан, линк, дата, делем стандартно как и в админке, а вот текстовое поле отображаем так.

### html

plain - пока просто текст ареа

### html

сверху две кнопочки `результат`(по умолчанию) `html`

Кнопочка html нажата подключаем ace. Смотри пример packages\dixipro\magicpro\admin\js\app\ArtEditor\component\AceEditor.vue (все что нужно для айс уже подключено шаблон)

### html

сверху две кнопочки `результат`(по умолчанию) `md`  
у нас пока нет преобразования md в html поэтому отображаем без преобразования
в качестве редактора используем textArea

1. Нужна только загрузка, фото уже обрезано
2. Нужно и загрузка и обрезка
   2.1 из файла
   2.2 из буфера обмена

да и
