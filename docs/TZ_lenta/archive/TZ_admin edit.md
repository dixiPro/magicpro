## отложенные вопросы

групповое удаление
Поиск
Порядок полей
хелп

    // id из маршрута: /article/id/5 или /article?id=5
    $id = (int) ($Get['id'] ?? 0);

    // Запись ищем в своей ленте: у чужой другие поля, и страница вышла бы пустой.
    //
    // first()   — сама лента 'products' или null, если такой нет
    // ?->       — на null вся цепочка даёт null, а не ошибку вызова у null
    // items()   — записи этой ленты: where feed_id + схема ленты передаётся
    //             запросу, поэтому у записи работают имена полей, а не __string_1
    // find($id) — where id внутри уже суженного запроса
    //
    // Вызвать find() прямо у ленты нельзя: это тот же класс Feed, и искать он
    // будет ленту с таким id, а не запись. FeedItem::find($id) тоже не годится —
    // вернёт запись любой ленты, с чужими полями.
    $item = $id ? Feed::where('code', 'products')->first()?->items()->find($id) : null;


    тыж трудяга, это лишнее я не могу скопировать, что ты мне первый раз написал, но добавиь в три строчки, там утебя все понятно было

->items()->find($id) Можно заменить на ->where('feed_id',$id) ?

$item = FeedItem::getById($id);
